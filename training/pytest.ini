[pytest]
# Add repo root to sys.path so absolute imports like
# `from training.data_prep.X import ...` resolve in headless CI.
pythonpath = ..
testpaths = tests data_prep/tests
markers =
    smoke: 1-epoch YOLO sanity check, run in CI on every training PR
