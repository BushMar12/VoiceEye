# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Auto-copied ONNX Runtime WASM files (regenerated by postinstall)
public/ort-wasm/

# Python
__pycache__/
*.pyc
venv*/
.venv/

# Training outputs
VoiceEye_Runs/
runs/
data/

# Model files (fetched from ClearML, not stored in git)
*.pt
*.pth
*.ckpt
*.safetensors
*.weights
*.onnx
# Track ONLY the deployed production model. The PWA loads it from
# /models/best.onnx (src/config.ts: YOLO_MODEL_PATH); the auto-promote
# workflow writes here too. Training scratch *.onnx files stay ignored.
!public/models/best.onnx

# OneDrive sync conflict files
*-The's MacBook Pro*
*-DESKTOP-*
*(conflict)*

# Claude Code local config
.claude/
CLAUDE.md
IMPLEMENTATION_PLAN.md
docs/

# Local env files (see .env.example for the schema)
.env
.env.local
.env.*.local

# Wrangler / Cloudflare Pages local state
.wrangler/

# Auto-downloaded binaries (cloudflared, etc.)
.bin/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
